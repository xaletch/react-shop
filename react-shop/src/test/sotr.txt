  const test = [2, 1, 4, 9, 7, 5];
  test.sort((a, b) => a - b)
  console.log(test)

  const test2 = ["а", 'я', 'э', 'б', 'у', 'п', 'в'];
  test2.sort((a, b) => (a < b ? -1 : 1));
  console.log(test2)

  =-=-=-=-=-=

  const data = [
    { name : 'Steve', age : 30 },
    { name : 'Bob', age : 23 }
  ];

  const data1 = [...data].sort((a, b) => (a.name < b.name ? -1 : 1))
  data1.map((d) => console.log(d.name));

  =-=

  const data1 = [...data].sort((a, b) => 
    a.name.toLowerCase() < b.name.toLowerCase() ? -1 : 1
  )
  data1.map((d) => console.log(d.name));